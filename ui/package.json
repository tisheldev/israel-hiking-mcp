{
  "name": "israel-hiking-trail-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Inline trail map view for the Israel Hiking MCP server, built as one self-contained MCP Apps document.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "leaflet": "^1.9.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.20",
    "@types/node": "^22.0.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^3.2.0"
  }
}
